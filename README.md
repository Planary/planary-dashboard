# Planary Dashboard

Standalone frontend dashboard for the Planary ecosystem. It is designed to be pushed as its own GitHub repo and deployed directly to Vercel.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`
